version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "mambaforge-4.10"

conda:
  environment: environment-dev.yml

python:
  install:
    - method: setuptools
      path: .

formats:
  - epub
  - pdf

sphinx:
  builder: html
  configuration: docs/conf.py
