version: 2

build:
  os: "ubuntu-26.04"
  tools:
    python: "miniforge3-26.3"

conda:
  environment: environment.yml

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

formats:
  - epub
  - pdf

sphinx:
  builder: html
  configuration: docs/source/conf.py
