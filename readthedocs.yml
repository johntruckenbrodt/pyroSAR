version: 2

build:
  image: latest

python:
  version: 3.6
  install:
    - requirements: requirements-dev.txt
    - method: setuptools
      path: .
  system_packages: true

formats:
  - epub
  - pdf
